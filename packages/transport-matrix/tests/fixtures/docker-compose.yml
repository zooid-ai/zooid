services:
  tuwunel:
    image: ghcr.io/matrix-construct/tuwunel:latest
    ports:
      - "8448:8448"
    volumes:
      - ./tuwunel.toml:/etc/tuwunel/tuwunel.toml:ro
      - ./registrations:/var/lib/tuwunel/registrations:ro
    environment:
      TUWUNEL_CONFIG: /etc/tuwunel/tuwunel.toml
