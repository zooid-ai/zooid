---
'@zooid/transport-matrix': patch
---

A human @mention of an agent inside a thread now switches the addressee instead of also triggering the thread's last-posting agent (or a task thread's assignee).
