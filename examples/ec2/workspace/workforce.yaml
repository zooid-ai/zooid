runtime: podman

transports:
  http-local:
    type: http
    port: 8080

agents:
  code:
    workdir: .
    acp:
      preset: claude
    http:
      transport: http-local
    container:
      image: ghcr.io/zooid-ai/budd-agent-claude-code:latest
